package com.youkeda.project.wechatproject.bot.context;

/** Identifies the model role receiving an assembled context package. */
public enum ContextAudience {
    ORCHESTRATOR,
    SUB_AGENT,
    DIRECT
}
